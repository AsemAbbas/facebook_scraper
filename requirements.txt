# ====================================
# مَرصَد · Multi-Source Facebook Monitor
# v4.0 - cPanel-ready with MySQL + Auth
# ====================================

# Backend Server
flask>=3.0.0           # web server + REST API
flask-cors>=4.0.0      # CORS للـ API
flask-login>=0.6.3     # session management

# Database (cPanel-compatible)
pymysql>=1.1.0         # pure-python MySQL driver (works on any cPanel)
cryptography>=41.0.0   # لتشفير API tokens في DB

# Core
pyyaml>=6.0.1          # قراءة config.yml
aiohttp>=3.9.0         # HTTP async requests (لكل الـ RSS sources و Apify)
requests>=2.31.0       # sync requests للـ Telegram + health checks

# Playwright (اختياري - فقط للتشغيل المحلي، ليس cPanel)
# ملاحظة: في cPanel استخدم Apify/FetchRSS/RSS.app بدل Playwright
playwright>=1.40.0

# Production WSGI (للنشر على VPS)
# استخدمه مع: gunicorn --workers 1 --threads 8 'server:app'
# لاحظ: --workers 1 ضروري لأن الجدولة (scheduler) تعمل في خيط داخلي واحد
gunicorn>=21.2.0

# Telegram (اختياري)
# requests موجود فوق

